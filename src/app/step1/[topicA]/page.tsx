'use client'

import styles from "./page.module.css";

export default function StepOneUrls() {
    return (
        <main className={styles.content}>
            Now add urls
        </main>
    );
}
